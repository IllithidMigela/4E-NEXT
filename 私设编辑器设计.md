# 私设条目编辑器设计文档

> 目标：以「威能（power）」为标杆，为全部 19 类私设条目建立「官方格式双向对齐 + 结构化编辑」的设计规范，逐类推进实现。
> 适用范围：`web/src/homebrew/EntryEditor.tsx`（表单）、`web/src/lib/homebrewSchema.ts`（schema）、`web/src/sheet/EntryCard.tsx`（预览）、`web/src/styles.extra.css`（样式）。
> 状态说明：power 为 **已实现标杆** ✅；其余类型处于「设计完成 / 待实现」阶段。

---

## 一、总体设计哲学（六条原则）

1. **官方格式双向对齐**——编辑所见即保存所得；官方条目（导入/模板）也能逆解析回编辑态。`<table class="details">` ↔ 标签块数组是最佳实践。
2. **统计驱动预设**——预设不拍脑袋，从官方语料（`dist/data/categories/*.json`）统计高频结构得出，用一次性脚本产出。
3. **主次分明**——核心面板常驻展开，次要面板收进底部「附加设置」折叠区（`core` 标记 + `extraOpen`）。
4. **防误操作**——非空内容套用预设需「再点一次确认替换」（`armPreset` + 2.5s 超时）。
5. **正交维度隔离**——含义独立的分类轴分开输入、各自派生（如威能的「再生频率」与「威能类型」）。
6. **实时预览**——右侧 `EntryCard` 完全复刻真实车卡渲染，空字段以虚线框（`Ghost`/`.frame-ghost`）呈现，可点击跳回左栏对应输入框（`goField`）。

---

## 二、威能（power）标杆设计（已实现）

### 2.1 界面骨架

左表单 / 右实时预览双栏（`hb-editor.double`，列宽 1.05fr : 1fr）。表单面板由 `CATEGORY_SECTIONS.power` 定义，8 个面板按 `core` 分两档：

| 档位 | 面板 | 说明 |
| --- | --- | --- |
| 核心（常驻） | 基本信息 · 威能 | 名称/英文名/出处/授予者/威能类型/等级，归属包固定在此 |
| 核心（常驻） | 使用频率 | 随意/遭遇/每日 三选 chip |
| 核心（常驻） | 威能详情（标签块） | 预设条 + 标签块编辑器 + 次攻击组 |
| 附加设置（折叠） | 风味文本 / 关键词 / 动作与射程 / 外观 / 标签 | 默认收起，标题栏显示面板清单，`grid-template-rows` 动画开合 |

### 2.2 数据模型：两个正交维度

| 维度 | 输入（form） | 保存派生 | 影响 |
| --- | --- | --- | --- |
| 再生频率 | `usageZh`：随意/遭遇/每日 | `powerFreqOf` → `usage`（at-will/encounter/daily） | 卡面色（绿/红/灰）+ 使用次数 |
| 威能类型 | `powerType`：攻击/辅助/特殊 | `powerKindOf` → `powerKind` | 卡头文字「战士攻击 1」 |

`buildEntry`（homebrewSchema.ts）在保存时分别派生，互不混淆。

### 2.3 各部分解析方式

- **基本信息**：纯标量进 `extras`；`grantedBy` 进卡头前缀。
- **威能详情（标签块）**——核心机制：
  - 数据结构 `PowerBlock = { label, text, indent }`，与官方 `<table class="details">` 的 `<th>标签：</th><td>内容</td>` 行一一对应。
  - `POWER_BLOCK_LABELS`：20 个标签（目标/攻击/命中/强化1/强化2/失手/效果/触发/要求/前提/代价/主目标/主攻击/次目标/次攻击/后效/次要维持/移动维持/标准维持/特殊），每个带填写 hint。
  - 编辑态存 JSON 字符串于 `form.powerBlocks`；`parsePowerBlocks` 还原数组。
  - 保存 `serializePowerBlocks` → 官方同款 HTML；缩进子行用 `\u00A0\u00A0` 前缀；无标签纯文本输出 `colspan=2` 行。
  - 导入官方条目 `parsePowerDetails` 反向解析 HTML（抓 `keyword` 小节与 `details` 表，`&nbsp;` 前缀识别缩进）——「从模板新建」直接得到可编辑标签块。
- **预设条**：`POWER_PRESETS` 23 个（攻击 12 / 辅助 9 / 特殊 2），来自 8903 条官方威能 details 行序列统计（攻击类 Top：`目标>攻击>命中` 885、`>失手>效果` 550、`>效果` 507；辅助类 Top：纯 `效果` 929、`目标>效果` 498、`触发>效果` 479）。空列表点击直接填入；非空需二次确认替换。
- **次攻击组**：连续 `indent>0` 行自动聚合为独立面板（bolt 图标），组内独立增删行、移行不出组；`POWER_TEMPLATE_SECONDARY` 一键插入官方固定结构。预设含缩进行（多段攻击/召唤傀儡）自动生成该面板；强化档位/移动维持用平铺行，避免误聚合。
- **动作与射程**：动作 8 按钮全宽单选；射程 `RANGE_TEMPLATES` 按 自身/近战/远程/爆发冲击/区域/其他 分组，模板 = 前缀 + 数字位，`composeRange` 组合、`parseRange` 正则反向回填，双向联动。
- **关键词**：`multichips` 顿号分隔，候选按 伤害类型/效果类型/附件类型/力量来源/其他 分组，悬停显示术语表解释。
- **保存派生**：extras 收集非空字段 → 派生 usage/powerKind → 有标签块时 `details` 完全由标签块生成（威能在 `WITHOUT_BODY`，无自由正文区）。

### 2.4 已实现的可复用资产（其他类型直接借用）

| 资产 | 位置 | 可复用场景 |
| --- | --- | --- |
| `PowerBlockEditor`（预设条 + 标签块 + 次攻击组） | EntryEditor.tsx | 物品威能、生物行动/特质、套装增益等一切「标签+内容」结构 |
| `serializePowerBlocks` / `parsePowerBlocks` / `parsePowerDetails` | homebrewSchema.ts | 任意 details 表结构的双向序列化 |
| `RANGE_TEMPLATES` + `composeRange`/`parseRange` | homebrewSchema.ts | 射程/距离类字段 |
| `multichips` + 分组候选（`groups`） | homebrewSchema.ts | 关键词/技能/来源等多选字段 |
| 主次分明（`core` + 附加设置折叠） | EntryEditor.tsx + styles.extra.css | 全类型 |
| 防误操作（二次确认） | PowerBlockEditor `armPreset` | 全类型预设 |
| 联动提示（`itemLevel`→增强→价格） | EntryEditor.tsx + levelprices.ts | 装备、仪式价格 |
| `Ghost` 虚线框预览 + `goField` 跳转 | EntryCard.tsx / EntryEditor.tsx | 全类型 |
| `splitThemeSections`（!!/!!! 章节树） | sheet/theme.ts | 主题/领域/职业/血统等正文含章节结构者 |
| `FeatRichText`（正文 [[链接]] 悬浮预览） | EntryCard.tsx | 所有含 [[威能]]/[[词条]] 链接的正文 |
| 官方数据语料 | `dist/data/categories/*.json` | 各类型高频结构统计脚本输入 |

---

## 三、其余 18 类设计分析

> 每类单独成文于 [私设设计/](私设设计/) 目录（按编辑复杂度排序），本节为索引与摘要：

| 文档 | 类型 | 复杂度 | 状态 |
| --- | --- | --- | --- |
| [01-装备-equipment.md](私设设计/01-装备-equipment.md) | 装备 | P1 | 设计完成（含官方数据统计） |
| [02-译名字典-dictionary.md](私设设计/02-译名字典-dictionary.md) | 译名字典 | P3 | 设计完成 |
| [03-术语-reference.md](私设设计/03-术语-reference.md) | 术语 | P3 | 设计完成 |
| [04-败德-vice.md](私设设计/04-败德-vice.md) | 败德 | P3 | 设计完成 |
| [05-美德-virtue.md](私设设计/05-美德-virtue.md) | 美德 | P3 | 设计完成 |
| [06-魔法学派-magic-school.md](私设设计/06-魔法学派-magic-school.md) | 魔法学派 | P2 | 设计完成 |
| [07-契约-pact.md](私设设计/07-契约-pact.md) | 契约 | P2 | 设计完成 |
| [08-血统-bloodline.md](私设设计/08-血统-bloodline.md) | 血统 | P2 | 设计完成 |
| [09-物品套装-item-set.md](私设设计/09-物品套装-item-set.md) | 物品套装 | P2 | 设计完成 |
| [10-仪式-ritual.md](私设设计/10-仪式-ritual.md) | 仪式 | P2 | 设计完成 |
| [11-领域-domain.md](私设设计/11-领域-domain.md) | 领域 | P1 | 设计完成 |
| [12-主题-theme.md](私设设计/12-主题-theme.md) | 主题 | P1 | 设计完成 |
| [13-传奇天命-epic-destiny.md](私设设计/13-传奇天命-epic-destiny.md) | 传奇天命 | P1 | 设计完成 |
| [14-典范之道-paragon-path.md](私设设计/14-典范之道-paragon-path.md) | 典范之道 | P1 | 设计完成 |
| [15-种族-race.md](私设设计/15-种族-race.md) | 种族 | P1 | 设计完成 |
| [16-专长-feat.md](私设设计/16-专长-feat.md) | 专长 | P1 | 设计完成（含句式统计） |
| [17-生物-creature.md](私设设计/17-生物-creature.md) | 生物 | P1 | 设计完成（含数据块结构） |
| [18-职业-class.md](私设设计/18-职业-class.md) | 职业 | P1 | 设计完成（分两步） |

> 每类按统一模板分析：卡片结构 → 数据模型 → 设计要点 → 解析方式 → 实现清单。
> 优先级：**P1**（结构化价值高、复用强）/ **P2**（中等）/ **P3**（轻量文本）。

### 3.1 装备 equipment（P1，接近完成）

- **卡片结构**（ItemCard）：头部（名称/类别·稀有度·L等级）+ 风味 + 统计表（分类组/增强/价格/重量/重击）+ 威能 + 正文详情。
- **现状**：字段分区已完成（基本信息·装备 / 统计数据 / 物品威能 / 外观 / 标签，前两者 core）；增强↔等级↔价格联动已完成（`enhancementBonusForLevel`）。
- **设计要点**：
  1. **物品威能 `power` 字段复用 PowerBlockEditor**——官方物品威能正文同为 details 表结构，`parsePowerDetails` 可直接逆解析；保存 `serializePowerBlocks`。将 `power` 从 longtext 改为结构化块（存 `powerBlocks` 形态，ItemCard 已能渲染）。
  2. 新增「物品威能预设」（基于官方物品威能统计）：每日攻击威能 / 遭遇辅助威能 / 自由动作触发等。
  3. 类别联动：武器 → 分类组候选收窄为武器组，护甲 → 护甲组（`GROUPS` 已有全量，需按 itemCategory 分组过滤）。
  4. 价格/重击格式 hint 已有；补「等级→稀有度建议」。
- **解析方式**：`power` 字段 HTML ↔ PowerBlock[] 双向；`itemLevel` 联动推导增强/价格（已完成）。
- **实现清单**：
  - [ ] power 字段改为标签块编辑器（复用 PowerBlockEditor + 预设）
  - [ ] 分类组按 itemCategory 过滤候选
  - [ ] 物品威能预设（统计脚本）

### 3.2 专长 feat（P1）

- **卡片结构**（FeatCard）：头部（层级·类型·出处）+ 前提 / 增益 / 特殊 三个固定标签块；正文区隐藏。
- **现状**：三个 longtext 分区（增益已 core）；`FeatRichText` 已支持正文 [[链接]] 悬浮预览。
- **设计要点**：
  1. 前提/增益/特殊 是**固定三个 label** 的块，不需 PowerBlockEditor 的任意标签——维持三 textarea，但可升级为「块编辑器」统一视觉（label 固定 + textarea）。
  2. **专长预设**：统计官方专长的高频句式/结构（前提句式：职业：X / 属性 X；增益句式：获得技能受训 / +2 某技能 / 获得专长武器），提供填空模板。
  3. 层级联动：tierZh 选择（英雄/典范/天命/史诗）。
- **解析方式**：纯文本字段 + [[链接]] 运行时解析（已有）；预设从 `dist/data/categories/feat.json` 统计句式。
- **实现清单**：
  - [ ] 专长预设（统计脚本 + 预设条）
  - [ ] （可选）三块统一视觉

### 3.3 种族 race（P1）

- **卡片结构**（GenericCard）：gc-fields（属性/体型/速度/视觉/技能）+ 风味 + 正文详情。
- **现状**：字段分区（基本信息 / 种族数据，均 core）+ 正文。
- **设计要点**：
  1. 种族数据枚举 chip 已有（`RACIAL_SIZES`/`SPEEDS`/`VISIONS`/`SKILLS`/`SIX_ABILITIES`）。
  2. **正文「种族威能」结构化**：官方种族正文含「种族威能」小节（起始/遭遇/每日威能）→ 复用 `sectionBetween` + 引用提取（theme.ts 同构逻辑），在编辑端提供「威能引用列表」结构化编辑：起始威能（必授）、遭遇/每日威能（等级+引用），自动生成 `{{威能名}}` 语法写回正文。
  3. 统计官方种族高频属性组合（如 力量+体质）、速度分布 → 候选排序。
- **解析方式**：正文 `!! 种族威能` 节 → 引用列表；角色页已有 `raceGrantedPowerEntries` 类解析可对齐。
- **实现清单**：
  - [ ] 通用「威能引用编辑器」组件（见阶段 1）
  - [ ] 种族威能分组引用编辑
  - [ ] 属性/速度候选统计排序

### 3.4 职业 class（P1，工作量最大）

- **卡片结构**（GenericCard gc-fields：职位/威能来源/关键技能）+ 正文；角色页另有完整的职业能力面板解析（`ClassFeatureBlock`：!! 职业特性 / !!! 1级：/ 威能授予 / 特性选择）。
- **现状**：字段分区 + 自由正文。
- **设计要点**：
  1. **职业正文结构化编辑**：复用 `splitThemeSections` 的章节树（!!/!!! 层级），编辑端实时预览章节树 + 折叠 lore。
  2. **威能授予结构化**：`!! 职业特性` 下按等级小节提取威能引用（起始威能/遭遇威能/每日威能），提供表格化编辑（等级 + 威能引用 + 数量）。
  3. **特性选择结构化**：多选特性（「获得N个…威能」）识别逻辑已在角色页成熟，编辑端可提示标注。
  4. 职业预设：从官方职业统计「威能来源 + 职责」组合建议。
- **解析方式**：正文章节树 + 威能引用提取（theme.ts 同构）；保存时把结构化编辑写回 wikitext/Markdown 正文。
- **实现清单**：
  - [ ] 章节树编辑组件（标题/层级/正文）
  - [ ] 职业威能表编辑（等级 × 引用）
  - [ ] 特性选择标注

### 3.5 典范之道 paragon-path（P1）

- **卡片结构**（GenericCard gc-fields：等级/前提）+ 正文。
- **现状**：字段分区（基本信息 / 典范条件）+ 正文。
- **设计要点**：
  1. 正文结构固定：起始特性（授予 2 个典范攻击威能）+ 11级特性 + 16级特性 → **与 theme 同构**，复用威能引用编辑器。
  2. 提供「典范威能」分组编辑（起始/11级/16级）。
- **解析方式**：`splitThemeSections` + 引用提取。
- **实现清单**：
  - [ ] 复用阶段 1 的威能引用编辑器

### 3.6 传奇天命 epic-destiny（P1）

- **卡片结构**（GenericCard gc-fields：层级/前提）+ 正文。
- **现状**：字段分区（基本信息 / 天命条件）+ 正文。
- **设计要点**：与典范之道几乎同构（天命特性 21级+ / 天命威能），共用模板，仅机制标题不同。
- **实现清单**：
  - [ ] 复用威能引用编辑器

### 3.7 物品套装 item-set（P2）

- **卡片结构**（GenericCard gc-fields：层级）+ 正文（套装组成 + 增益）。
- **现状**：字段分区 + 正文。
- **设计要点**：
  1. **套装增益结构化**：「2件套 / 3件套 / 4件套」→ 固定块编辑器（label=件数，content=增益文本），或复用 PowerBlockEditor 变体。
  2. 套装组成：[[物品]] 链接多选（复用 multichips + 链接生成）。
- **解析方式**：正文 `!! 套装组成` / `!! 套装增益` 节拆分（theme 同构）。
- **实现清单**：
  - [ ] 件数增益块编辑器
  - [ ] 套装组成链接多选

### 3.8 仪式 ritual（P2）

- **卡片结构**（GenericCard gc-fields：仪式等级/类别/关键技能）+ 正文。
- **现状**：字段分区（基本信息 / 仪式信息）+ 正文。
- **设计要点**：
  1. **头部字段补全**：官方仪式卡固定头部为 等级/类别/时间/花费/市场价/关键技能 + 效果。新增 `time`、`cost`（市场价）字段，全部字段化抽离正文。
  2. 价格 hint：按仪式等级查价表（复用 `priceForLevel`）。
  3. 类别枚举已有 `RITUAL_CATEGORIES`。
- **解析方式**：官方仪式条目的头部行（`类别：` / `时间：` / `花费：`）→ 字段提取，导入时自动填充。
- **实现清单**：
  - [ ] 新增 time/cost 字段 + 面板
  - [ ] 官方仪式头部逆解析

### 3.9 主题 theme（P1）

- **卡片结构**：主题简介 + 章节树（扮演/创建 lore 折叠、起始特性起机制展开）+ 起始/额外/可选三个威能列表（角色页 `ThemeChapters` 已完整实现）。
- **现状**：字段分区 + 自由正文；`splitThemeSections` / `themeStarting` / `themeExtraPowers` / `themeOptionalPowers` 解析逻辑已在角色页成熟。
- **设计要点**：
  1. **编辑端章节树实时预览**：复用 `splitThemeSections`，正文输入时右侧显示章节结构（lore 折叠 / 机制展开）。
  2. **威能引用三段式编辑**：起始特性 / 额外特性 / 可选威能 三个分组，每节 = 等级标题 + 引用列表，保存时生成对应 `!!`/`!!!` 标题 + `{{威能}}` 语法。
- **解析方式**：`splitThemeSections` + `extractRefs`；保存反向拼装正文。
- **实现清单**：
  - [ ] 章节树预览组件
  - [ ] 三段式威能引用编辑

### 3.10 领域 domain（P1）

- **卡片结构**：类似主题（领域特性 + 领域威能）；角色页有 `warpriestDomains` 解析（领域种类：引导神力/遭遇/特性）。
- **现状**：字段分区 + 自由正文。
- **设计要点**：与主题同构（机制起始标题为「领域威能」而非「起始特性」），共用模板；可选「领域威能种类」标注（引导神力/遭遇/特性）。
- **实现清单**：
  - [ ] 复用主题模板

### 3.11 魔法学派 magic-school（P2）

- **卡片结构**：GenericCard + 正文（学派威能列表）。
- **设计要点**：正文含「学派威能」小节（攻击/辅助/通用，等级引用列表）→ 复用威能引用编辑器。
- **实现清单**：
  - [ ] 复用威能引用编辑器

### 3.12 契约 pact（P2）

- **卡片结构**：GenericCard + 正文（契约威能：起始契约威能 / 遭遇 / 每日）。
- **设计要点**：与血统/主题同构（契约威能引用列表 + 契约特性）。
- **实现清单**：
  - [ ] 复用威能引用编辑器

### 3.13 血统 bloodline（P2）

- **卡片结构**：GenericCard + 正文（血脉威能）。
- **设计要点**：与主题同构（血脉特性 + 血脉威能引用列表）。
- **实现清单**：
  - [ ] 复用威能引用编辑器

### 3.14 生物 creature（P1，数据块价值高）

- **卡片结构**：GenericCard（`gen-creature-card` 直接渲染原生 HTML 数据块）。
- **现状**：自由正文（含 `<div class=creature>` 原生 HTML）。
- **设计要点**：
  1. **生物数据块结构化**：官方生物条目 = 数据块（等级/角色/体型/HP/速度/AC/强韧/反射/意志/技能/豁免）+ 特质 + 行动。拆成字段面板（等级/角色/体型等枚举）。
  2. **行动/特质 = 标签块**：复用 PowerBlockEditor（行动：标签「行动」/「特质」+ 描述），保存序列化。
  3. 高价值：结构化后可直接驱动战斗模拟器 / 怪物数据渲染。
- **解析方式**：官方生物 HTML 数据块 → 字段/块逆解析（`parsePowerDetails` 变体或专用解析器）。
- **实现清单**：
  - [ ] 生物数据块解析器（官方 HTML → 字段）
  - [ ] 行动/特质标签块编辑器（复用）

### 3.15 败德/美德 vice · virtue（P3）

- **卡片结构**：GenericCard + 正文（获取方式 + 效果）。
- **设计要点**：结构简单，保持正文为主，轻量补充「关联威能」引用编辑。
- **实现清单**：
  - [ ] 可选：关联威能引用

### 3.16 术语 reference（P3）

- **卡片结构**：GenericCard + 正文。
- **设计要点**：纯文本为主，低优先级；可选「关联词条」多选（[[链接]] 生成）。
- **实现清单**：
  - [ ] 可选：关联词条多选

### 3.17 译名字典 dictionary（P3）

- **卡片结构**：GenericCard + 正文。
- **设计要点**：nameEn 已承载英文名；可加「词性/来源」可选字段。低优先级。
- **实现清单**：
  - [ ] （可选）词性字段

---

## 四、实施路线图

### 阶段 1：通用能力建设（一次投入，8+ 类型受益）

1. **通用「威能引用编辑器」组件**——段落引用提取（`{{名称}}`/`[[名称]]`）+ 分组列表编辑 + 回写正文。服务于：race / theme / domain / bloodline / pact / magic-school / class / paragon-path / epic-destiny。
2. **PowerBlockEditor 复用扩展**——装备 `power` 字段、生物行动/特质、物品套装增益。
3. **专长/装备预设统计脚本**——`feat.json` 句式统计、`equipment.json` 威能结构统计。

### 阶段 2：复杂类型

4. 职业章节树 + 威能表编辑（class）。
5. 生物数据块解析与编辑（creature）。
6. 仪式头部字段化（ritual）+ 官方头部逆解析。

### 阶段 3：轻量收尾

7. vice / virtue / reference / dictionary 轻量化补充。

---

## 五、状态追踪

| 类型 | 字段分区 | core 标记 | 结构化编辑 | 预设 | 联动 | 状态 |
| --- | --- | --- | --- | --- | --- | --- |
| power 威能 | ✅ | ✅ | ✅ 标签块+次攻击组 | ✅ 23 个 | ✅ 频率/类型 | **已完成** |
| equipment 装备 | ✅ | ✅ | 待（power 字段） | 待 | ✅ 等级/增强/价格 | 设计完成 |
| feat 专长 | ✅ | ✅ | 三块已拆 | 待统计 | — | 设计完成 |
| race 种族 | ✅ | ✅ | 待（种族威能） | — | — | 设计完成 |
| class 职业 | ✅ | ✅ | 待（章节树/威能表） | — | — | 设计完成 |
| paragon-path 典范之道 | ✅ | ✅ | 待（威能引用） | — | — | 设计完成 |
| epic-destiny 传奇天命 | ✅ | ✅ | 待（威能引用） | — | — | 设计完成 |
| item-set 物品套装 | ✅ | ✅ | 待（件数增益块） | — | — | 设计完成 |
| ritual 仪式 | ✅ | ✅ | 待（头部字段化） | — | 待（价格） | 设计完成 |
| theme 主题 | ✅ | ✅ | 待（章节/威能三段） | — | — | 设计完成 |
| domain 领域 | ✅ | ✅ | 待（复用 theme） | — | — | 设计完成 |
| magic-school 魔法学派 | ✅ | ✅ | 待（威能引用） | — | — | 设计完成 |
| pact 契约 | ✅ | ✅ | 待（威能引用） | — | — | 设计完成 |
| bloodline 血统 | ✅ | ✅ | 待（威能引用） | — | — | 设计完成 |
| creature 生物 | ✅ | ✅ | 待（数据块/行动） | — | — | 设计完成 |
| vice 败德 | ✅ | ✅ | 可选 | — | — | 设计完成 |
| virtue 美德 | ✅ | ✅ | 可选 | — | — | 设计完成 |
| reference 术语 | ✅ | ✅ | 可选 | — | — | 设计完成 |
| dictionary 译名字典 | ✅ | ✅ | 可选 | — | — | 设计完成 |

> 注：`core` 标记已在 `CATEGORY_SECTIONS` 全部 19 类就位（基本信息 + 主体数据面板）。
