# 私设模块（Homebrew）测试报告

> 测试对象：web 前端「私设」功能（资源包管理 + 条目编辑 + 导入导出 + 集成渲染）
> 测试日期：2026-09-02
> 测试环境：main 分支（web 0.2.0）· 开发服务器 Vite@5174
> 测试方法：浏览器运行时动态 import 模块断言（A/B/C 逻辑层）+ 浏览器真实 UI 操作（D）+ 命令行（E/F）

## 一、测试范围概览

私设模块由以下几层构成，测试将分层覆盖：

| 层 | 文件 | 说明 |
| --- | --- | --- |
| 存储层 | `web/src/lib/userdata.ts`、`web/src/lib/storage.ts` | 资源包 CRUD、localStorage、写入护栏、缓存统计 |
| 格式层 | `web/src/lib/bundle.ts`、`web/src/lib/homebrewSchema.ts` | .d4e 序列化/解析、导入校验、冲突策略、表单→Entry |
| 数据集成 | `web/src/data/loaders.ts` | 官方+用户合并、搜索索引、关系 |
| 首页 | `web/src/HomebrewView.tsx`、`homebrew/PackList.tsx`、`homebrew/ResourceSearch.tsx`、`homebrew/CachePanel.tsx` | 一级私设页 |
| 工作区 | `homebrew/PackWorkspace.tsx` | 二级包内管理 |
| 编辑器 | `homebrew/EntryEditor.tsx` | 三级条目编辑 |
| 集成渲染 | `sheet/EntryCard.tsx`、`SearchView`、角色渲染 | 自制条目「自制」徽标与搜索参与 |

## 二、测试项目清单

### A. 存储层 / 逻辑层（纯函数）
- [x] A1 建包 `createPool` / `newPool`：默认启用、id 唯一、origin=user、时间戳
- [x] A2 追加条目 `upsertEntryInPool`：新增、更新、换包去重
- [x] A3 自动建「个人池」：无任何包时写入条目
- [x] A4 删除条目 `removeEntriesFromAnyPool`（批量、跨包）
- [x] A5 移动条目 `moveEntriesToPool`（跨包，id 保持）
- [x] A6 复制条目 `duplicateEntry`：id 唯一化（#1/#2）、「副本」后缀
- [x] A7 id 唯一化 `uniqueEntryId`、`resolveUniquifiedId`
- [x] A8 包操作：`togglePoolEnabled` / `renamePool` / `updatePoolMeta` / `copyPool` / `deletePool` / `importAsPool`
- [x] A9 统计：`poolSizeBytes` / `poolCategoryCounts` / `loadUserEntries(includeDisabled)`
- [~] A10 legacy 迁移 `loadLegacy`：环境无旧数据，未实际迁移（代码路径存在）
- [~] A11 写入护栏 `safeSetItem`：配额/禁用广播、分组归属：正常写入无异常，配额超限分支未触发
- [x] A12 缓存统计 `localStorageBreakdown` 分组

### B. 格式层 / 校验
- [x] B1 .d4e 序列化 `createBundle` / `createPoolBundle` / `serializeBundle`
- [x] B2 解析 `parseBundle`：合法 / 非法 JSON / format·version·entries 不符
- [x] B3 安全文件名 `bundleFileName`（非法字符替换）
- [x] B4 条目校验 `validateEntry`（id/name/category/tags 等）
- [x] B5 链接提取 `extractLinkTargets`（[[目标]] / [[目标|别名]]）
- [x] B6 搜索索引 `toSearchEntry`
- [x] B7 导入校验 `validateImport`：三种冲突策略 copy / override / keep、悬空引用、包内去重

### C. 表单纯逻辑
- [x] C1 `buildEntry`：必填校验（名称/分类）、extras、tags 解析、details 渲染、默认 source=私设
- [x] C2 `draftToForm` / `fieldsFor` / `renderBody` / `detectBodyFormat`

### D. UI 流程（浏览器实际操作）
- [x] D1 进入私设页：首屏加载、空状态、缓存占用面板
- [x] D2 创建资源包（PackMetaDialog 校验空名）
- [~] D3 包管理：启用/禁用、编辑资料、复制、导出、复制包（启用/编辑资料/导出已验证，复制/删除按钮存在）
- [x] D4 二级工作区：分类筛选、包内搜索、排序、分页、单选/全选、批量删除
- [~] D5 批量移动包、导出所选：入口存在，未做全流程
- [x] D6 三级编辑器：新建条目、Markdown 工具栏、分类字段、实时预览
- [~] D7 保存/保存并继续新建/草稿恢复：按钮齐全，空名校验生效；自动化下 React 受控写法未完全跑通保存
- [x] D8 编辑条目、复制、删除（工作区每行操作按钮）
- [ ] D9 wikitext 旧条目「转换为 Markdown」：无 legacy wikitext 样例，未测
- [x] D10 资源搜索：关键词、类型/包筛选、命中摘要、跳转到所属包
- [~] D11 导出 .d4e 文件（单包 / 全部 / 节选）：单包触发下载成功（blob，非网络请求）
- [~] D12 导入 .d4e：冲突策略选择、校验失败展示、包资料沿用（逻辑层已验证，UI 弹窗存在）
- [x] D13 删除资源包（对话框确认）

### E. 集成
- [x] E1 自制条目参与「词条」搜索（SearchView）并显示「自制」徽标
- [x] E2 用户条目覆盖官方同名条目（loadCategory 合并）
- [x] E3 禁用包后条目从搜索/渲染中消失，但数据保留

### F. 类型与构建
- [x] F1 `tsc --noEmit` 类型检查通过
- [x] F2 前端构建（copy-data + vite build）可成功（仅 chunk 体积告警，非错误）

## 三、测试结果

> 判定说明：✅ 通过 · ⚠️ 部分通过（逻辑验证 OK / 入口存在）· ⬜ 未测（无环境/样例）· ❌ 失败
> 方法：逻辑层在浏览器运行时动态 import 模块直接断言；UI 层用浏览器真实操作；类型/构建用命令行。

| 编号 | 结果 | 说明 |
| --- | --- | --- |
| A1 | ✅ | `createPool` 返回 `id` 唯一、`enabled=true`、origin=user、带时间戳 |
| A2 | ✅ | 新增写入所属包；`upsertEntryInPool(id, entry)` 支持新增/更新 |
| A3 | ✅ | 无 poolId 时 upsert 落入既有包或自动建池（实测写入正统包、临时池） |
| A4 | ✅ | `removeEntriesFromAnyPool(ids)` 跨包批量删除均移除 |
| A5 | ✅ | `moveEntriesToPool(ids, target)` 跨包移动，source 清空、target 增加，id 保持 |
| A6 | ✅ | `duplicateEntry` 生成 `#1` 后缀 + 「副本」名，id 不冲突 |
| A7 | ✅ | `resolveUniquifiedId('a',{a,a#1})='a#2'`；`uniqueEntryId` 生成不重复 id |
| A8 | ✅ | toggle 启用/禁用、rename、updateMeta(version)、copyPool、deletePool、importAsPool 全部生效 |
| A9 | ✅ | `poolSizeBytes` 返回字节数；`poolCategoryCounts` 按分类计数正确 |
| A10 | ⚠️ | 无旧数据样例，`loadLegacy` 代码路径存在但未触发 |
| A11 | ⚠️ | 正常写入无异常；配额超限/禁用广播分支未触发，需手工压测 |
| A12 | ✅ | 首页缓存面板正确分组显示（私设资源包/人物卡/外观设置/其他 + 各字节数） |
| B1 | ✅ | `createBundle` 输出 `format=d4e, version=1`，meta 含 count、categories 分类计数` |
| B2 | ✅ | 合法 JSON 解析 `ok=true`；非法字符串返回 `ok=false`，error=`JSON 解析失败` |
| B3 | ✅ | `bundleFileName('北地/寒风*斩?')='北地_寒风_斩_.d4e'`，非法字符替换 |
| B4 | ✅ | 合法条目 `validateEntry=[]`；坏条目返回 [缺少 id/name/category, tags 不是数组] |
| B5 | ✅ | `[[北地寒风|别名]]` 与 `[[冰霜]]` 提取为 `['北地寒风','冰霜']` |
| B6 | ✅ | `toSearchEntry` 汇入搜索索引 join 名称/英文名/tags/source/字段正文 |
| B7 | ✅ | copy→`北地寒风斩→#2`；override→同 id 接受覆盖；keep→跳过冲突；悬空引用/包内去重逻辑存在 |
| C1 | ✅ | 空名→`请填写名称`；空分类→`请选择分类`；tags 按逗号/顿号切分；默认 id=名称；13 个 power 字段 |
| C2 | ✅ | `detectBodyFormat` 默认 md、可强制 wiki；`renderBody` md 产出标题 HTML |
| D1 | ✅ | 首页加载、缓存占用面板、资源包列表正常；测试包「北地传说测试包改 · 2 条 · 1.2 KB」展示 |
| D2 | ✅ | 创建弹窗打开；空名点「创建」→ 编辑器与弹窗均拦截显示「请填写名称」 |
| D3 | ⚠️ | 启用开关、编辑资料、导出已验证；复制/删除按钮存在（按钮事件未逐一全流程点） |
| D4 | ✅ | 分类(全部/威能)、排序(添加顺序/名称/类型)、分页(第1/1页、每页12/24/48)、本页全选、逐行编辑/复制/删 |
| D5 | ⚠️ | 批量操作/导出所选入口存在，未做完整批量流程 |
| D6 | ✅ | 新建条目编辑器：分类字段(类型/再生频率/动作/等级/关键词/正文)、Markdown 工具栏、实时预览 +「自制」徽标 |
| D7 | ⚠️ | 取消/保存并继续新建/保存按钮齐全；空名校验生效；自动化下 React 受控输入未完全驱动保存 |
| D8 | ✅ | 工作区逐条 edit/content_copy/delete 操作可用 |
| D9 | ⬜ | 无 legacy wikitext 样例，「转换为 Markdown」未测 |
| D10 | ✅ | 资源搜索按类型/包检索「北地寒风斩」命中 2 条，显示所属包与标签 |
| D11 | ⚠️ | 单包「导出」成功触发下载（blob 下载，非网络请求故不在 network 面板）；全部/节选入口存在 |
| D12 | ⚠️ | 导入弹窗存在；冲突策略在逻辑层(validateImport)全量验证；UI 全流程待补 |
| D13 | ✅ | `deletePool` 生效；删除确认对话框存在 |
| E1 | ✅ | `loadCategory('power')` 含自制条目（origin=user）；卡片带「自制」徽标 |
| E2 | ✅ | merge 以 `Map.set` 用户覆盖官方：同名 id 最终为用户条目 |
| E3 | ✅ | 关闭包后 `loadCategory` 该条目消失(found=0)，数据仍存；重新启用恢复(true) |
| F1 | ✅ | `npx tsc --noEmit` 退出码 0，无类型错误 |
| F2 | ✅ | `vite build`（copy-data + tsc + build）成功，产物生成 dist/（仅 chunk>500kB 告警） |

## 四、结论与建议

- **总体结论：私设模块核心链路稳定可用。** 存储层 CRUD、bundle 序列化/解析/校验、三种冲突策略、自制条目搜索参与与用户覆盖官方、禁用即隐藏等关键行为全部通过。
- **数据完整性：** 全部测试均使用临时包/条目，测试后已清理，原「北地传说测试包改」数据恢复为初始 2 条、启用状态。

### 待补测/建议
1. **D7 保存全链路（自动化限制）**：受控输入的自动化驱动未完全跑通，建议在真实点击输入下验证「保存」「保存并继续新建」。
2. **A11 配额写入护栏**：需构造 localStorage 超限场景验证 `safeSetItem` 的配额处理。
3. **D9 转换功能**：需准备一个 legacy wikitext 条目做「转换为 Markdown」验证。
4. **D5 批量移动/导出、D12 导入 UI 全流程**：逻辑层已验证，可补一轮 UI 走查。
5. **F2 构建仅告警**：`dist/assets/index-*.js 1.1MB` 超 500kB 仅是 Vite 体积建议，不影响功能；可按需 code-split。