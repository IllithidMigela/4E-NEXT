# 装备（equipment）私设编辑器设计

> 优先级：**P1**（复用强、离完成最近）· 状态：**设计完成，待实现**
> 关联代码：`homebrewSchema.ts`（字段/分区/序列化）、`EntryEditor.tsx`（表单）、`EntryCard.tsx` ItemCard（预览）、`styles.extra.css`
> 数据依据：`dist/data/categories/equipment.json`（2603 条官方装备，2026-09-07 统计）

---

## 一、卡片结构（右侧预览 ItemCard）

```
┌─────────────────────────────────────────────┐
│ [图标] 名称 NameEn      [自制] 类别 · 稀有度 · L等级 │
│ 风味文本（来自正文第一段）                        │
│ 统计表：分类组 / 增强 / 价格 / 重量 / 重击          │
│ 威能（专属区块，仅自制条目渲染）                   │
│ 正文详情（details HTML）                        │
└─────────────────────────────────────────────┘
```

**现状差异**：官方装备的威能信息承载在 `details`（底部「正文详情」区块，`<div class="bold bg-item">威能…</div>` 结构）；自制条目走专属「威能」区块（`entry.power`）。两者渲染位置不同——结构化后统一走「威能」区块，官方格式双向对齐。

## 二、数据模型

### 2.1 现有字段（CATEGORY_FIELDS.equipment）

| 字段 | 类型 | 说明 | 状态 |
| --- | --- | --- | --- |
| itemCategory 类别 | select | 19 种官方类别 | ✅ |
| itemLevel 物品等级 | text | 联动增强/价格 | ✅ |
| rarity 稀有度 | select | 普通/非普通/稀有/神之碎片 | ✅ |
| group 分类组 | multichips | 武器/护甲组 | ✅（未按类别过滤） |
| enh / cost / weight / critical | text | 统计表 | ✅（enh/cost 联动） |
| power 威能 | longtext | **待升级为结构化** | ⏳ |

### 2.2 缺失字段（需新增）

| 字段 | 依据 | 建议 |
| --- | --- | --- |
| **itemSuitable 适合** | 官方 2603 条中广泛存在（法杖87/权杖84/圣徽82/任意77/法珠75/盾牌68/近战63/魔杖61/护腕55/基地物品49） | multichips，候选取官方 Top，可自由输入；渲染在卡头 meta 或详情首行 `类别：适合` |

### 2.3 官方类别分布（候选顺序参考）

法器512 · 武器361 · 奇物304 · 护甲295 · 头部148 · 颈部147 · 臂部134 · 戒指124 · 消耗品125 · 足部118 · 手部90 · 腰部75 · 炼金物品42 · 另类奖励29 · 弹药23 · 刺客毒药19 · 机关附件15 · 坐骑13 · 龙晶强化10 · 伙伴6 · 魔宠2

## 三、面板设计（主次分明）

`CATEGORY_SECTIONS.equipment` 调整建议：

| 面板 | core | 说明 |
| --- | --- | --- |
| 基本信息 · 装备 | ✅ | 名称/英文名/出处/类别/稀有度/等级/适合 |
| 统计数据 | ✅ | 分类组/增强/价格/重量/重击 |
| **物品威能** | **✅（升级）** | 74% 装备含威能，属主体内容，应收进常驻区 |
| 外观 | — | 附加设置 |
| 标签 | — | 附加设置 |

## 四、物品威能结构化（核心新增）

### 4.1 官方格式（已实测）

```html
<div class="bold bg-item">威能（恐惧）✦每日（自由动作）</div>
<div class=text>触发：你使用此武器以诗人攻击威能命中一个敌人。<br>效果：…</div>
```

- 段头 = `威能（关键词）✦频率（动作）`，关键词可缺省（`威能✦每日（标准动作）`）
- 正文 = `<div class=text>` 行，多为纯文本，少数带内联 `<b>标签：</b>`（统计：特殊28 / 强化1 19 / 强化2 9 / 次要维持 1）

### 4.2 数据模型

```ts
interface ItemPowerSection {
  keywords?: string;                 // 威能（关键词）可缺省
  freq: "每日" | "遭遇" | "随意" | "消耗";  // 含装备专属「消耗」
  action: string;                    // 自由动作/次要动作/标准动作/即时中断/即时反应/无动作/移动动作
  blocks: PowerBlock[];              // 正文标签块（复用 20 标签 + 自由文本行）
}
```

### 4.3 编辑交互

- 「＋ 添加威能段」逐段添加（官方最多 4 段/件）
- 每段 = **段头行**（关键词 multichips 小候选 + 频率 chip + 动作 chip，官方风格顺序「✦频率（动作）」）+ **正文区**（复用 `PowerBlockEditor` 标签块）
- 频率候选：每日/遭遇/随意/消耗（`ITEM_FREQUENCIES`，区别于威能类的三值，多出「消耗」）
- 动作候选：官方 7 种 + 无动作

### 4.4 序列化 / 反序列化（官方格式双向对齐）

- `serializeItemPowerSections(sections)` → 官方 `<div class="bold bg-item">` 段头 + `<div class=text>` 行（内联标签输出 `<b>标签：</b>` 前缀；无标签纯文本直接输出）
- `parseItemPowerSections(html)` → 逆解析官方 HTML：按 `bg-item` 段头切段，段头解析 关键词/频率/动作，正文行解析内联标签
- `draftToForm` 补装备分支：导入官方条目时，把 details 里的 增强/重击/特性/威能段 逆解析回对应字段/段列表

### 4.5 预设系统（统计驱动，数据已产出现成）

基于 2100 个官方威能段的高频组合：

| 预设名 | 结构 | 依据 |
| --- | --- | --- |
| 每日 · 自由动作 · 触发 | 频率每日 + 自由动作 + 正文（触发/效果） | 每日1392 × 自由动作664 最高频 |
| 每日 · 次要动作 · 效果 | 每日 + 次要动作 + 效果 | 次要动作569 |
| 遭遇 · 标准动作 · 攻击 | 遭遇 + 标准动作 +（目标/攻击/命中） | 标准动作385 |
| 随意 · 标准动作 · 攻击 | 随意 + 标准动作 + 攻击块 | 随意155 |
| 消耗 · 自由动作 | 消耗 + 自由动作（消耗品专属） | 消耗205 |
| 医疗 · 每日 | 关键词预填医疗 | 关键词 Top1：医疗102 |
| 传送 · 每日 | 关键词预填传送 | 传送90 |

段头关键词候选按官方 Top 排序：医疗102 / 传送90 / 毒素44 / 可强化38 / 幻术37 / 火焰35 / 咒法33 / 区域32 / 魅惑31 / 光耀31 / 恐惧27 / 心灵19。

## 五、联动规则（已有 + 新增）

| 联动 | 状态 |
| --- | --- |
| itemLevel → 增强加值区间（enhancementBonusForLevel） | ✅ 已有 |
| itemLevel → 价格提示（priceForLevel） | ✅ 已有 |
| itemCategory → 分类组候选过滤（武器→武器组 / 护甲→护甲组） | ⏳ 待实现 |
| 类别 → 稀有度建议（消耗品多为普通） | 可选 |

## 六、实现清单

- [ ] `homebrewSchema.ts`：`ITEM_FREQUENCIES`（含消耗）、`itemSuitable` 字段 + 候选、`ItemPowerSection` 模型、`parseItemPowerSections` / `serializeItemPowerSections`
- [ ] `EntryEditor.tsx`：威能段编辑器组件（段头行 + PowerBlockEditor 复用 + 段预设条 + 二次确认）
- [ ] `CATEGORY_SECTIONS`：物品威能面板标 `core: true`
- [ ] `draftToForm`：官方装备 details 逆解析回填（增强/重击/适合/威能段）
- [ ] 分类组按 itemCategory 过滤（GROUPS 分组化）
- [ ] `ItemCard`：itemSuitable 渲染（卡头 meta 或详情首行）
- [ ] 清理临时脚本 `_tmp_equip_analysis.mjs`

## 七、开放问题

1. 威能段正文是否完全复用 20 标签集，还是收敛为物品高频标签（触发/效果/命中/特殊/强化1/强化2/维持）？——建议：复用全量，下拉候选把高频标签排前。
2. 一个段内多段威能（如「威能✦每日」+「威能✦随意」）编辑顺序是否需固定按官方（每日在前）？——建议不强制，按用户添加顺序。
